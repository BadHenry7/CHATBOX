services:
  - type: web
    name: rasa-bot
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "rasa run --enable-api --cors '*' --debug --port 5005 --host 0.0.0.0 --endpoints endpoints.yml"
    envVars:
      - key: PORT
        value: "5005"
